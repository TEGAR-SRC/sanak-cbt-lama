# Example environment configuration for ZYA CBT / CodeIgniter
# Copy this file to .env and adjust values for your environment.
# Lines beginning with # are comments.

# Application Environment (production | development | testing)
CI_ENV=development

# Base URL override (leave empty to auto-detect logic in config.php)
# BASE_URL=http://localhost/cbt1/

# Database Settings
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cbt1
DB_USER=root
DB_PASS=
DB_DRIVER=mysqli
# Optional DSN (overrides individual parts if set)
# DB_DSN=

# Table prefix (optional)
# DB_PREFIX=cbt_

# Encryption key override (leave blank to use value in config.php)
# ENCRYPTION_KEY=change_me_generate_random_string

# Custom site configuration
# SITE_NAME=Computer Based-Test
# SITE_VERSION=1.0.0

# PHP memory/time tweaks (optional, only if needed)
# PHP_MEMORY_LIMIT=512M
# PHP_MAX_EXECUTION_TIME=120

# Any additional custom variables can be added below...
